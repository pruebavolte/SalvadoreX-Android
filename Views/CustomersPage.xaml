<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:SalvadoreXAndroid.ViewModels"
             x:Class="SalvadoreXAndroid.Views.CustomersPage"
             Title="Clientes"
             BackgroundColor="#F8FAFC">

    <Grid RowDefinitions="Auto,*" Padding="10">
        
        <!-- Header -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="0,0,0,10">
            <SearchBar Grid.Column="0" 
                       Placeholder="Buscar cliente..."
                       Text="{Binding SearchText}"
                       BackgroundColor="White"/>
            
            <Button Grid.Column="1"
                    Text="+ Nuevo"
                    BackgroundColor="#22C55E"
                    TextColor="White"
                    CornerRadius="8"
                    Margin="10,0,0,0"
                    Command="{Binding AddCustomerCommand}"/>
        </Grid>
        
        <!-- Customers List -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Customers}"
                        SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItem Text="Editar" 
                                           BackgroundColor="#3B82F6"
                                           Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:CustomersViewModel}}, Path=EditCustomerCommand}"
                                           CommandParameter="{Binding .}"/>
                                <SwipeItem Text="Eliminar" 
                                           BackgroundColor="#EF4444"
                                           Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:CustomersViewModel}}, Path=DeleteCustomerCommand}"
                                           CommandParameter="{Binding .}"/>
                            </SwipeItems>
                        </SwipeView.RightItems>
                        
                        <Frame BackgroundColor="White" 
                               Margin="0,5" 
                               Padding="15"
                               CornerRadius="10">
                            <Grid ColumnDefinitions="Auto,*">
                                <Frame Grid.Column="0"
                                       BackgroundColor="#3B82F6"
                                       CornerRadius="25"
                                       WidthRequest="50"
                                       HeightRequest="50"
                                       Padding="0">
                                    <Label Text="{Binding Name, Converter={StaticResource InitialsConverter}}"
                                           TextColor="White"
                                           FontSize="18"
                                           FontAttributes="Bold"
                                           HorizontalTextAlignment="Center"
                                           VerticalTextAlignment="Center"/>
                                </Frame>
                                
                                <VerticalStackLayout Grid.Column="1" Margin="15,0,0,0">
                                    <Label Text="{Binding Name}" 
                                           FontSize="16" 
                                           FontAttributes="Bold"/>
                                    <Label Text="{Binding Phone}" 
                                           FontSize="13" 
                                           TextColor="#6B7280"/>
                                    <Label Text="{Binding Email}" 
                                           FontSize="12" 
                                           TextColor="#6B7280"/>
                                </VerticalStackLayout>
                            </Grid>
                        </Frame>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
    
</ContentPage>
